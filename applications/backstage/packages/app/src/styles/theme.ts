import { lightTheme, darkTheme } from '@backstage/theme';

// IA-Ops Custom Theme Configuration
// Using base themes for now, can be extended later
export const iaOpsLightTheme = lightTheme;
export const iaOpsDarkTheme = darkTheme;
